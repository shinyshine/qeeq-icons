
import React from 'react'
import QeeqIcon from '../IconWrap';

const <%= identifier %>Svg = <%= content %>;

const <%= identifier %> = (
  props,
  ref
) => <QeeqIcon {...props} ref={ref} icon={<%= identifier %>Svg} />;

<%= identifier %>.displayName = '<%= identifier %>';
export default React.forwardRef(<%= identifier %>);
